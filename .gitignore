node_modules/
.next/
.vercel/
*.log
tsconfig.tsbuildinfo

# Secrets: every env file is ignored...
.env
.env.*
env.local
env.example

# ...except the placeholder template, which must never contain a real key.
!.env.example
